[pytest]
testpaths = evals
asyncio_mode = auto
markers =
    destructive: posts/deletes real tweets (deselect with -m "not destructive")
    browser: needs CDP browser connection
    llm: needs Z.ai LLM access
    network: needs internet (HN/RSS)
    slow: takes > 5 minutes
